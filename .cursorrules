# 项目：UAC 绕过快速启动器 (Tauri + Rust)

## 核心技术准则
1. **权限逻辑**: 禁止直接请求管理员权限。必须通过 Rust 调用 `schtasks` 创建/运行计划任务来实现 UAC 绕过。
2. **快捷方式处理**: 必须支持解析 `.lnk` 文件以提取真实路径（建议使用 `parselnk` 库）。
3. **UI 规范**: 模仿 Windows 开始菜单风格。支持“大图标”、“小图标”和“列表”视图切换。
4. **交互**: 核心交互是 `tauri-drop` 文件拖拽识别。

## 开发偏好
- 保持回答简明扼要。
- 后端逻辑放在 `src-tauri/src` 下，保持模块化。
